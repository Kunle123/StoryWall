# Prefer explicit production=false over deprecated npm "production" flag (quiets
# "Use --omit=dev instead" on Railway/Nixpacks). Keeps devDependencies for next build
# (types, ESLint). Do not add omit=dev here — that would skip devDeps on all installs.
production=false
